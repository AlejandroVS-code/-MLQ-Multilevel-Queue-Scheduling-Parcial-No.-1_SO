services:
  dev:
    build: .
    container_name: mlq-scheduler-dev
    volumes:
      - .:/app
    working_dir: /app
    # Mantiene el contenedor vivo para que puedas "entrar" con exec
    tty: true
    stdin_open: true
